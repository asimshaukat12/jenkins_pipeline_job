pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
            sh '''
            pwd
            ls
            date
            tail /etc/passwd
            '''
            }
        }
    }
}
